# SML Subset Interpreter Requirements
# 
# Complete interpreter for SML subset with:
# - Recursive functions support (fun keyword)
# - Static type checking with ArrowType
# - Higher-order functions and closures
# - Error detection (Parse/Type/Def errors)
# - 49/50 tests passing (98% success rate)

# Core Requirements
# This interpreter is built with Python standard library only
# No external dependencies required for basic functionality

# Minimum Python version required
python>=3.8

# Optional dependencies for development and testing
pytest>=6.0.0     # For running comprehensive test suite
pytest-cov>=2.0.0 # For test coverage analysis
black>=21.0.0     # For code formatting
flake8>=3.8.0     # For linting and style checks
mypy>=0.800       # For static type checking

# Documentation dependencies (optional)
markdown>=3.0.0   # For rendering documentation